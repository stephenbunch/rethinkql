{
  "name": "rethinkql",
  "version": "0.1.0",
  "description": "Observable queries.",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -rf lib && tsc --outDir lib --declaration --sourceMap",
    "pre-jest": "rm -rf test && tsc --outDir test",
    "jest": "jest",
    "test": "npm run pre-jest && npm run jest"
  },
  "repository": {
    "url": "https://github.com/stephenbunch/rethinkql",
    "type": "git"
  },
  "author": "Stephen Bunch",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^15.1.32",
    "babel-preset-es2015": "^6.18.0",
    "jest": "^16.0.1",
    "typescript": "^2.0.6"
  },
  "jest": {
    "coverageDirectory": "coverage/",
    "collectCoverage": true,
    "testPathDirs": [
      "test/"
    ]
  }
}
