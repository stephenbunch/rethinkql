{
  "name": "rxql",
  "version": "0.1.0",
  "description": "Observable query language.",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -rf lib && tsc --outDir lib --declaration --sourceMap",
    "pretest": "rm -rf test && tsc --outDir test --module commonjs",
    "test": "mocha test/**/*.spec.js",
    "test-debug": "mocha test/**/*.spec.js --debug-brk"
  },
  "repository": {
    "url": "https://github.com/stephenbunch/rxql",
    "type": "git"
  },
  "author": "Stephen Bunch",
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^3.4.34",
    "@types/mocha": "^2.2.32",
    "@types/rethinkdb": "^1.10.30",
    "babel-preset-es2015": "^6.18.0",
    "chai": "^3.5.0",
    "mocha": "^3.1.2",
    "rethinkdb": "^2.3.3",
    "typescript": "^2.0.6"
  }
}
